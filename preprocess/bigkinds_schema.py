"""빅카인즈 내보내기 엑셀의 컬럼 정의와 정규화 규칙.

빅카인즈 표준 내보내기는 19개 컬럼이다. 분석에 쓰는 것만 추려 아래 이름으로 옮긴다.

주의 두 가지가 있다.

1. `본문`은 내보내기 단계에서 **200자로 잘린다**(전체 기사 93%가 정확히 200자).
   따라서 토픽모델링 입력으로는 본문 대신 `키워드`/`특성추출`을 쓴다.
   이 두 컬럼은 빅카인즈가 **원문 전체**를 형태소 분석해 만든 결과라 절삭 영향을 받지 않는다.
2. `뉴스 식별자`는 `02100311.20210131095312001` 형태의 문자열이다.
   dtype 지정 없이 읽으면 pandas가 float으로 해석해 정밀도가 날아간다.
   이 모듈은 항상 `dtype=str`로 읽는다.
"""

from __future__ import annotations

# 원본 → 정규화 컬럼명
COLUMN_MAP: dict[str, str] = {
    "뉴스 식별자": "article_id",
    "일자": "date",
    "언론사": "source",
    "제목": "title",
    "통합 분류1": "category1",
    "통합 분류2": "category2",
    "통합 분류3": "category3",
    "키워드": "keywords",
    "특성추출(가중치순 상위 50개)": "features",
    "본문": "content",
    "URL": "url",
    "분석제외 여부": "exclude_flag",
}

USECOLS: list[str] = list(COLUMN_MAP)

# 내보내기 단계에서 본문이 잘리는 길이. 절삭 여부 판정에만 쓴다.
CONTENT_TRUNCATION_LEN = 200

# 빅카인즈가 자체적으로 분석 제외를 표시한 값. 쉼표로 여러 개가 붙는다("중복, 예외").
EXCLUDE_TOKENS = ("예외", "중복")

# 1회 내보내기 상한. 행 수가 정확히 이 값이면 초과분이 잘렸을 수 있다.
EXPORT_ROW_CAP = 20_000
