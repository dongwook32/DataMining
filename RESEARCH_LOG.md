# 연구 일지

한국형 경제 이슈 국면 탐지 연구의 진행 기록입니다. 논문 3~5장(데이터·방법론·결과)을 쓸 때
"왜 이렇게 했는지"를 되짚을 수 있도록, 결정과 그 근거를 남깁니다.

**최신 항목이 위**에 옵니다.

## 기록 기준

모든 작업을 적지 않습니다. 아래에 해당할 때만 씁니다.

- 데이터 수집·전처리 산출물이 새로 생기거나 내용이 바뀐 경우
- 분석 방법·모형·변수 정의를 정하거나 바꾼 경우
- 연구 설계에 영향을 주는 발견 (데이터의 성질, 한계, 외부 사건)
- 분석 결과가 나온 경우
- 되돌린 결정과 그 이유

문서 정리, 오타 수정, 리팩터링, 배운 것 없는 실패는 적지 않습니다.
코드·파일 단위의 기술적 변경 내역은 `.cursor_progress.log`에 따로 남깁니다.
이 문서는 연구의 흐름을, 그쪽은 작업의 흐름을 담습니다.

---

## 2026-09-21 — docs 통합·비전공자용 요약·노션 단순화

### 배경
`docs/`에 pipeline·functions·glossary·notion 초안·긴 research_plan이 겹쳐 최신 Step 4~5
상태를 한곳에서 읽기 어려웠다. 비전공자용 설명 MD와 노션 요약이 필요했다.

### 수행
- 신규 `docs/연구_쉬운_요약.md` (파이프라인·알고리즘·폴더·코드·결과 요지).
- `docs/기술_참고.md`로 pipeline·functions·glossary 통합 후 해당 3파일 삭제.
- `docs/research_plan.md`는 PDF·차별점·RQ만 남김. 긴 초안은 `docs/_archive/research_plan_초안.md`
  (git HEAD 복원). `feasibility_report.md`는 `_archive/`로 이동.
- `docs/README.md` 인덱스, 루트 `README.md` 링크 갱신. `build_results_report.py` 출처 경로 수정.
- 노션 페이지 `3b283350…` 본문을 위 요약 구조로 `replace_content`.

### 결과
문서 읽기 순서: `연구_쉬운_요약` → `기술_참고` → `results.md`. 노션은 동일 요지의 짧은 버전.

### 다음
입력·분석이 바뀌면 `results.md`·QC·PDF·이 요약을 같은 순서로 맞춘다.

---

## 2026-09-19 10:27 — 폴백 BERTopic 비교 (본분석 NMF 유지)

### 배경

NMF 6국면에서 주 종속 3개의 KW가 약했고, LDA는 4그룹으로 축이 줄어 코스피만
유의했다. BERTopic을 같은 폴백 칸에 넣어 6국면 반응이 모형만 바꾸면 살아나는지
보기로 했다. 본국면은 바꾸지 않는다.

### 수행

`analysis.fallback_bertopic`: `doc_term` 등장 어휘를 공백으로 이은 문자열에
`paraphrase-multilingual-MiniLM-L12-v2`를 올리고, 월당 1,000건 무작위 층화
(67,000건, seed 42)에 BERTopic(nr_topics=14, min_topic_size=40)을 적합했다.
전량 변환은 하지 않았다. 매핑은 NMF와 같은 키워드 질량, 수동 덮어쓰기 없음.
반응은 `measure_reactions.analyze`를 `reactions_bertopic`에 썼다.
그룹이 1개면 KW를 계산하지 않게 `group_tests`를 고쳤다.

기각한 선택: 본국면을 BERTopic으로 교체하지 않음. 키워드 가방 임베딩이 시황
어휘로 뭉친 결과를 국면 6개로 억지 재라벨하지 않음.

### 결과

재현: `python -m analysis.fallback_bertopic`. 임베딩 656.6480297999951s, 적합 합계
683.1406986999937s. HDBSCAN 토픽 6개, 이상치 48/67000. 요청 14보다 적음
(`bertopic_meta.json`). 행렬 sha256은 NMF와 같음
`ceb9483da92f409928619a4bfda24dc140adcdf6073c321f0319b87dc1f28cff`.

토픽 0·1·2·4·5 → 증시실적, 토픽 3 → 기타 (`topic_regime_map.csv`).
z-우세 67개월 전부 증시실적. KW는 k=1이라 결측. 전환은 topics 신호 2024-02
한 건(증시실적→증시실적).

### 연구 설계에 미치는 영향

이 설정에서 BERTopic은 NMF가 나눈 6국면을 재현하지 못했다. 코스피 KW가
강해지는지를 같은 라벨 구조로 비교할 수 없다. “다른 알고리즘을 써도 RQ3는
약하다”의 근거로 쓰지 않는다. 본분석은 NMF로 둔다.

### 다음

- 본문 임베딩·전량 변환은 하지 않았다. 비용이 커서 이번에는 안 했다.

---

## 2026-09-19 09:38 — Step 4~5 반응·강건성·RQ4와 조건부 LDA 폴백

### 배경

국면 시계열(NMF, z-우세)과 월 패널은 이미 있었다. 논문용 필수 산출 중 반응표·전환
전후·강건성·검색 정합이 없었고, 통합 진입점도 없었다. 본분석 알고리즘은 NMF로
유지하고, 주 종속변수 그룹 차이가 약할 때만 LDA를 켜기로 했다. ENSI·감성 비교와
raw 재수집은 하지 않기로 했다.

### 수행

`processed` 캐시만 읽었다. `analysis.measure_reactions`로 6국면(물가 0개월) 그룹
비교·KW·비중/더미 HAC·전환 ±3개월을 만들었고, `robustness`로 2026-01 이후 7개월을
뺀 재추정, `rq4_search`로 뉴스 비중·라벨과 데이터랩 검색을 맞췄다.
`run_pipeline`이 이 순서를 묶는다.

폴백 트리거는 주 종속 3개(kospi_ret, usdkrw_ret, ccsi_diff) KW가 모두 p≥0.05이거나
2026 제외에서 그 유의 여부가 뒤집힐 때다. 전자가 성립해 LDA(online, 14토픽, 표본
201,000, seed 42, max_iter 15)를 같은 `doc_term.npz`에 적합했다. 매핑 규칙은 NMF와
같되 토픽 번호가 달라 NMF의 topic 3 수동 덮어쓰기는 넣지 않았다. BERTopic은
패키지가 없어 건너뛰었다.

기각한 선택: 본국면을 LDA로 교체하지 않음. 포화 회귀를 본결과로 올리지 않음.

### 결과

재현: `python -m analysis.run_pipeline` (종료 코드 0, 경과 498.6s, 그중 LDA.fit+transform 490.63194450001174s).

NMF 본분석 KW (`regime_tests.csv`): kospi_ret H=8.527401658920496 p=0.12946540630153042;
usdkrw_ret H=1.4278825432687938 p=0.9212390971597227; ccsi_diff H=5.89205337576037
p=0.31686473210419364; base_rate_diff H=13.211952271720104 p=0.021471472012258595;
kospi_ret_std H=9.92737970984237 p=0.07731925491451119. Holm 쌍 0/60.
금리 국면 kospi_ret 평균 -3.73788236578104 (n=10), AI반도체 +7.738230536251667 (n=13).

2026 제외 60개월: 주 3개와 base_rate_diff의 p<0.05 여부는 안 뒤집힘
(`robustness_compare.csv`). kospi_ret p는 0.3042894090839844.

RQ4 뉴스 비중–검색 z Pearson: 금리 0.524986317017003, 부동산 0.5582036473886888,
가계대출 0.4211041078805555, 대외통상 0.33789074546753217, AI반도체 0.7505857678829452,
증시실적 0.11345264061465014. 물가는 뉴스 비중 분산 0. 검색 z 라벨 일치 22/67
= 0.3283582089552239 (`rq4_concordance.csv`).

LDA 폴백: 매핑에서 물가·금리·가계대출이 빠짐. z-우세는 부동산 17 · 대외통상 18 ·
AI반도체 14 · 증시실적 18. kospi_ret KW H=11.810272660425 p=0.00806216709795685
(4그룹). 환율·CCSI는 p≥0.05. 행렬 sha256은 NMF와 동일
`ceb9483da92f409928619a4bfda24dc140adcdf6073c321f0319b87dc1f28cff`.

산출: `data/processed/reactions/`, `regimes_lda/`, `reactions_lda/`.
색인 `docs/results.md`. (2026-09-21 이후) 용어·파이프라인은 `docs/기술_참고.md`.

### 연구 설계에 미치는 영향

단일 라벨 6그룹으로는 코스피·환율·CCSI 반응 차이를 KW로 주장하기 어렵다.
기준금리 차분만 그룹 차이가 있다. 검색은 AI반도체·부동산·금리 축에서 뉴스 비중과
같이 움직이므로 RQ4 보조는 본분석 국면을 폐기할 이유가 되지 않는다.

LDA에서 코스피 KW가 작아진 것은 금리·가계대출을 못 나눠 4그룹이 된 결과다.
같은 RQ3(6국면)의 대체 답이 아니다. 본분석 라벨은 NMF로 둔다.

### 다음

- 논문 초고는 이 저장소 범위 밖. 표는 `docs/results.md`가 가리키는 CSV를 쓴다.
- BERTopic은 설치·임베딩 비용이 남아 있어 이번에는 안 돌렸다.

---

## 2026-09-15 00:40 — 빅카인즈 엑셀의 JSON 사본과 잘못 선언된 시트 dimension

### 배경

앞선 작업에서 `data/raw`의 CSV를 JSON으로 옮겼고, 이어서 빅카인즈 엑셀도 같이 해 달라는
요청을 받았다. 형식 변환 자체는 연구 내용을 바꾸지 않지만, 검증 과정에서 엑셀 파일의
메타데이터 결함을 발견해 기록한다.

### 수행

`preprocess/bigkinds_to_json.py`를 추가해 `data/raw/bigkinds` 아래 엑셀 61개를 레코드 배열
JSON으로 변환했다. 원본은 삭제하지 않았다.

두 가지를 정했다. 첫째, 컬럼은 19개 전부를 원본 한글 이름 그대로 옮긴다.
`bigkinds_schema.COLUMN_MAP`의 정규화·선별은 적용하지 않았다. 이 산출물은 형식만 바꾼
사본이고, 컬럼을 고르는 일은 `build_corpus.py`의 몫이기 때문이다. 둘째, 전 컬럼을
`dtype=str`로 읽는다. `bigkinds_schema` 모듈 주석대로 `뉴스 식별자`를 dtype 지정 없이
읽으면 float으로 해석되어 정밀도가 날아간다.

검증은 두 경로로 했다. (1) 스크립트 내부에서 기록 직후 JSON을 다시 읽어 대조,
(2) pandas를 거치지 않고 openpyxl `read_only`로 셀을 직접 훑어 전 셀 비교.

2차 검증이 처음에는 61개 중 6개만 통과했다. 실패한 55개는 헤더가 1열, 행 수가 0으로
읽혔다. 원인은 변환이 아니라 검증 코드였다. 엑셀의 `xl/worksheets/sheet1.xml`을 직접 열어
보니 55개가 `<dimension ref="A1"/>`로, 즉 데이터가 한 칸뿐인 것처럼 선언돼 있었다
(2021년 12, 2022년 12, 2024년 12, 2025년 12, 2026년 7). 나머지 6개는 2023년 파일 전부이고
`A1:S28184`처럼 올바르게 선언돼 있었다. openpyxl의 `read_only` 모드는 이 선언을 그대로
믿는다. `ws.reset_dimensions()`로 무시하게 한 뒤 61개 전부 통과했다.

### 결과

61개 파일, 총 998,020행 × 19열. 1,071.7MB → 2,830.7MB(디스크 실측 2.764GB).
1·2차 검증 모두 불일치 0.

재현: `python -m preprocess.bigkinds_to_json`

여기서의 998,020행은 중복·예외 제거 전 원본 행 수다. 기존에 기록한 코퍼스 931,703건과
다른 이유는 확인하지 않았다 — `build_corpus.py`의 중복 제거·분석제외 처리 결과일 것으로
보이나 대조하지 않았다.

### 연구 설계에 미치는 영향

`build_corpus.py`는 `pd.read_excel`을 쓰고 `read_only`를 켜지 않는다. `read_excel`은
선언된 dimension을 믿지 않으므로(dimension이 `A1`인 파일에서 14,711행을 정상적으로
읽었다) 기존 코퍼스는 이 결함에 영향받지 않는다. 다만 앞으로 속도 때문에 `read_only`나
다른 엑셀 리더로 바꿀 일이 있으면 행이 조용히 사라질 수 있으니 `reset_dimensions()`가
필요하다.

### 다음

- `data/processed`의 CSV 13개는 아직 변환하지 않았다

---

## 2026-09-14 16:30 — 원시 CSV의 JSON 사본과 float 파싱 정밀도 문제

### 배경

`data/raw`의 CSV를 JSON으로도 쓸 수 있게 해 달라는 요청을 받았다. 데이터 자체는
그대로 두고 형식만 바꾸는 작업이므로 연구 내용에는 영향이 없다고 보았으나,
변환 과정에서 원본과 값이 어긋나는 경우가 나와 기록한다.

### 수행

`preprocess/csv_to_json.py`를 추가하고 `data/raw` 아래 CSV 5개를 레코드 배열 JSON으로
변환했다. 원본 CSV는 삭제하지 않고 옆에 `.json`을 함께 두었다.

첫 변환 뒤 표준 라이브러리 `csv`/`json`으로 전 셀을 대조했더니 `datalab_regime_daily.csv`의
`score_rel` 262개 값이 원본과 달랐다. 예: CSV의 `19.949400436046517`이 JSON에서
`19.94940043604652`가 됐다. 차이는 약 3e-15로 배정도 실수 1 ULP 수준이다.

원인은 두 군데다. 하나는 `pandas.read_csv`의 기본 C float 파서가 정확 반올림을 하지
않는 것(`float_precision="round_trip"`으로 해결), 다른 하나는 `DataFrame.to_json`의
기본 `double_precision=10`이 16자리 값을 자르는 것(`json.dumps`로 대체). 두 가지를
모두 고친 뒤 재변환했다.

### 결과

5개 파일 전부 변환·검증 통과. 행 수는 각각 `datalab_regime_daily` 27,055,
`datalab_regime_monthly` 889, `datalab_sub_daily` 108,220, `datalab_sub_monthly` 3,556,
`ecos_monthly` 127이다. 검증은 두 경로로 했다. (1) 스크립트 내부에서 기록 직후 JSON을
다시 읽어 행 수·열 이름·값 대조, (2) 스크립트와 무관하게 표준 라이브러리 `csv`로 원본을
읽어 전 셀 비교. 2차 검증에서 불일치 0.

재현: `python -m preprocess.csv_to_json`

### 연구 설계에 미치는 영향

`preprocess/build_panel.py`는 같은 CSV들을 `float_precision` 지정 없이 읽는다. 즉 현재
패널의 `score_rel` 파생값에도 같은 1 ULP 오차가 들어가 있을 수 있다. 크기가 상대오차
1e-16 수준이라 국면 탐지·반응 분석 결과를 바꿀 가능성은 낮다고 보지만, **실제로
바뀌는지는 확인하지 않았다.** 패널을 다시 만들 일이 있으면 그때 함께 확인한다.

### 다음

- `build_panel.py`의 `read_csv`에도 `float_precision="round_trip"`을 적용할지 판단
- `data/processed`의 CSV와 BigKinds `.xlsx`는 변환하지 않았다 (요청 범위 밖)

---

## 2026-09-07 00:45 — 국면 탐지: 전량 변환 NMF와 z-우세 라벨

### 배경

전처리로 `doc_term.npz`와 월 패널이 준비됐다. 국면 시계열은 탐색용
`topic_monthly_share.csv`(월별 앞 3,000건)로 확정할 수 없다고 이미 정해 둔 상태였다.
언론사 범위는 경제지 3곳으로 유지하고, 결과를 경제지 기준 이슈 국면으로 읽기로 했다.

### 수행

IDF는 전량 931,703건(n_terms=0 6건 제외)에서 계산하고, NMF(14토픽, CD, nndsvd, seed 42)는
월당 3,000건 무작위 층화표본 201,000건에 적합했다(iter 93, 재구성 오차 437.307617).
추정 H로 전량 문서를 변환해 월별 토픽 비중을 만들었다.

MiniBatchNMF 전량 적합은 버렸다. `max_no_improvement=10` 때문에 3 epoch 만에 멈추고
성분이 ‘핵심·환경·경쟁력’ 같은 일반 어휘로 남았다. 탐색 단계와 같은 `sklearn.decomposition.NMF`를
쓰는 편이 맞다고 판단했다. 적합 표본은 탐색과 같은 규모이되, parquet 앞부분을 자르지 않고
월 안에서 무작위로 뽑았다.

국면 라벨은 원비중 argmax가 아니라 7국면 비중의 표본 내 z점수 argmax다. 원비중 최댓값은
67개월 전부 증시실적이었다. 경제지 코퍼스에서 증시 기사는 매달 많아서, ‘평소보다 오른 축’이
국면 질문에 맞다. 원비중 열은 `dominant_raw`로 남겨 두었다.

토픽→국면은 데이터랩 키워드 세트와 H 질량의 겹침이다. 토픽 3(현대차·기아·SUV)은 검색 세트에
전기차가 있어 AI반도체로 붙었으나 상위 어휘가 완성차라 `기타`로 수동 덮었다.

변화점은 PELT L2, 페널티 `dim × ln(n)`, 최소 구간 3개월. 원비중 [0,1]에 이 페널티를 그대로
씌우면 전환이 0건이었다. 열별 z점수 뒤에 같은 식을 적용했다. 페널티 값은 2026년에 맞춰
바꾸지 않았다.

### 결과

산출: `data/processed/regimes/` (`regime_monthly.csv`, `topic_monthly_share.csv`,
`changepoints.csv`, `regimes_qc.md`). 재현: `python -m analysis.detect_regimes`.

z-우세 개월: 부동산 15, AI반도체 13, 대외통상 11, 증시실적 10, 금리 10, 가계대출 8, 물가 0.
피크 월(원비중): 금리 2022-09 (0.11057657), 부동산 2021-12 (0.08810681),
가계대출 2022-10 (0.076264985), 대외통상 2025-04 (0.12821142),
AI반도체 2026-06 (0.14146186), 증시실적 2022-02 (0.255156981).
토픽 11(AI·반도체)과 탐색 T10의 월별 비중 상관은 +0.979.

국면 신호 전환(BIC): 2022-03 부동산→금리, 2023-02 금리→가계대출, 2025-02 가계대출→대외통상,
2025-11 대외통상→AI반도체. 2026-03과 2026-07은 안 잡혔다. 타당성 확인으로 기대한 두 달은
이 페널티에서 전환이 아니다.

정합: 67개월, 결측 0, n_docs 합 931,703, 국면+잔차 행합 최소 0.999999880000 · 최대 1.000000104000.
행렬 sha256 `ceb9483da92f409928619a4bfda24dc140adcdf6073c321f0319b87dc1f28cff`.

### 연구 설계에 미치는 영향

전량 NMF에서 물가와 금리는 한 토픽(9)이다. 금리 H 질량 3.3567256927490234, 물가 3.340873956680298.
검색 대분류의 물가는 뉴스 토픽에서 독립 축이 아니다. Step 4의 국면 그룹은 뉴스 라벨 6개
(금리·부동산·가계대출·대외통상·AI반도체·증시실적)로 가는 것이 데이터와 맞다. 물가 검색 시계열은
RQ4 보조 검증에 그대로 둔다.

원비중 우세는 Step 4 라벨로 쓰지 않는다. z-우세와 원비중이 다른 월이 57/67이다.

2026-03·07이 뉴스 국면 전환으로 안 잡힌 것은 지표 충격과 이슈 축 이동이 같은 달이 아닐 수
있다는 뜻이다. 반응 분석에서 그 두 달을 국면 전환 이벤트로 넣지 말고, 이미 적어 둔 대로
변동성 강건성(kospi_ret_std, 2026 제외)으로 다룬다.

### 다음

국면 라벨을 `month`로 월 패널에 조인 → 국면별 반응표 (`data/processed/reactions`).
z-우세 6국면 그룹 비교를 우선하고, 포화 회귀는 뒤로 둔다.

---

## 2026-09-04 17:15 — 전처리 완료: 문서-어휘 행렬과 월 패널

### 배경

Step 2에서 뉴스 코퍼스 931,709건, ECOS 지표 127개월, 데이터랩 검색 7국면·28세부를 확보했다.
다만 세 소스가 서로 다른 형태로 흩어져 있어 국면 탐지(Step 3)와 반응 분석(Step 4)에
바로 넣을 수 없었다. 연구계획서 4절 플로우의 [3] 전처리에 해당하는 작업을 했다.

### 수행

**문서-어휘 행렬.** 코퍼스 전량을 28,343개 어휘의 이진 희소행렬로 만들었다
(`preprocess/build_doc_matrix.py` → `doc_term.npz`, 931,709 × 28,343, 비영 7,947만, 밀도 0.30%).
어휘는 문서빈도 200 이상·등장비율 15% 이하로 걸렀다.

기존 `topic_monthly_share.csv`를 쓰지 않기로 한 것이 이 단계의 핵심 결정이다. 그 파일은
월별로 앞에서 3,000건씩 잘라 만든 201,000건 표본인데, 무작위가 아니라 parquet 등장 순서대로
앞부분만 취한 것이다. 행이 날짜·언론사순으로 정렬돼 있어 월초 기사와 특정 매체가 과대표집된다.
국면 대분류 7개를 도출하는 탐색 용도로는 충분했지만, 국면 시계열을 확정하는 입력으로는
쓸 수 없다고 판단했다.

값을 빈도가 아닌 이진(등장/미등장)으로 둔 것도 결정이다. 빅카인즈 `키워드` 컬럼은 형태소
분석 결과를 중복 없이 나열한 것에 가까워 빈도 정보를 신뢰하기 어렵다. TF-IDF 가중은
모형을 돌리는 쪽에서 얹는다.

**월 패널.** ECOS·데이터랩·코퍼스 뉴스량을 한 달 격자에 합쳤다
(`preprocess/build_panel.py` → `monthly_panel.csv`, 127개월 × 54열).
레벨 변수는 대부분 비정상이라 가격류(코스피·환율·CPI)는 로그차분, 지수·금리류(CCSI·기준금리)는
단순차분으로 파생변수를 만들었다. 물가는 기저효과 탓에 전월비가 계절을 타므로 전년동월비를
함께 뒀다. 검색은 국면마다 원값·전기간 z점수·7국면 내 점유율·로그변화율·월내 일별 표준편차
다섯 개를 뒀다. z점수는 절대 관심 수준을, 점유율은 국면 간 관심 배분을 보는 것이라
서로 다른 질문에 답한다.

패널 기간은 2016-01부터 남겼다. 분석 창(2021-01~2026-07) 첫 달의 차분·전년동월비를 만들려면
lead-in이 필요하다. 분석 창 여부는 `in_study` 열로 구분한다.

### 결과

분석 창 67개월 내 결측 0. 두 번 실행해 산출물 해시가 동일한 것을 확인했다.
행렬은 원본에서 토큰을 재추출해 복원 결과가 정확히 일치하는 것을 확인했다.

행렬에서 15만 건을 무작위 추출해 TF-IDF + NMF 14토픽을 시험 삼아 돌린 결과, 토픽이
7국면에 대응하고 피크 시점이 실제 사건과 맞았다. 물가·금리 토픽은 2022-09(인플레 정점),
부동산은 2021-12(집값 정점), 가계대출은 2023-01, AI반도체는 2026-06에 14.8%로 최고치다.
표본 기반 탐색 결과보다 축이 선명하다.

### 연구 설계에 미치는 영향

**분석 창 끝에 극단적 사건이 두 개 있다.** 2026-03 미·이란 중동전쟁 발발(유가 100달러·환율
1,500원 돌파, 코스피 -19.1%)과 2026-07 대폭락(-22.19%, 8,476.48 → 6,595.45)이다.
처음에는 ECOS 수집 오류를 의심했으나 보도된 월말 종가와 대조해 실제 값임을 확인했다.

그 결과 코스피 월수익률 표준편차가 구간별로 6.02(2021~22) / 4.58(2023~24) / 6.44(2025) /
22.03(2026 상반기)이다. 환율과 심리지수는 같은 정도로 흔들리지 않았으므로 코스피 고유의
분산 급증이다. 원계열을 그대로 회귀에 넣으면 마지막 7개월이 국면 계수를 지배한다.
대응으로 `kospi_ret_std`(직전 12개월 변동성으로 표준화, 분모에 시차를 걸어 당월 정보 누출
차단)를 강건성용 종속변수로 추가했다. Step 4에서는 원계열과 표준화 계열을 나란히 보고하고,
2026년 제외 표본을 강건성 검증에 넣기로 한다.

**같은 사건이 검색에도 찍혀 있다.** 2026-03 물가 검색이 전월 7.5에서 154.25로 뛰었다(z=+8.5).
`score_rel`은 앵커로 나눈 값이라 앵커가 무너져도 치솟을 수 있어 확인했는데, 그달 앵커는
0.648로 중앙값 0.699 근처였고 원점수가 2016년 이후 최고치인 100을 찍은 것이었다.
정규화가 깨진 것이 아니라 실제 검색 급증이다. 대외통상도 같은 달 최고치, 증시실적은
2026-07 최고치다. 뉴스·검색·지표가 같은 사건을 가리킨다는 점에서 RQ4(검색 트렌드의 보조 검증)에
유리한 재료다.

**통계 검정 해석에 주의할 점.** 코스피 로그수익률의 ADF가 AIC 기준으로 단위근을 기각하지
못한다(p=0.695). 시차 0으로 고정하면 p=0.0000이다. 관측치 67개에서 AIC가 시차 3을 잡아
자유도를 소모한 소표본 문제이지 단위근이 아니다. 품질 리포트가 두 결과를 함께 보고하도록 했다.

### 남은 문제

- 코퍼스 언론사가 매일경제·서울경제·한국경제 3개 경제지뿐이다. 매체 편향 통제가 불가능하므로
  종합지를 추가 투입하거나 "경제지 기준 이슈 국면"으로 주장 범위를 좁혀야 한다. 미결.
- 관측치 67개월에 국면 더미 7개와 통제변수를 넣으면 자유도가 빠듯하다. Step 4는 포화 회귀보다
  국면별 그룹 비교를 우선하는 편이 안전하다.

### 다음

1. `doc_term.npz` 전량에 NMF/LDA → 월별 토픽 비중 → 국면 라벨 (`data/processed/regimes`)
2. ruptures로 변화점 탐지. 2026-03·2026-07이 잡히면 발견이 아니라 타당성 확인으로 읽을 것
3. 국면 라벨을 `month`로 월 패널에 조인 → 국면별 반응표 (`data/processed/reactions`)
