# doc_term_vocab.csv — 컬럼 설명

`doc_term.npz` 행렬의 **열** 정의. 행렬을 읽을 때 반드시 같이 읽어야 한다.

| 컬럼 | 설명 |
|------|------|
| col | 행렬의 열 번호 (0부터, 파일 순서와 같음) |
| term | 어휘 |
| df | 그 어휘가 등장한 코퍼스 문서 수 (= 해당 열의 합) |

`keyword_df.csv`의 `df`는 어휘 필터를 걸기 전 값이라 여기 `df`와 다를 수 있다.
분석에는 이 파일 쪽을 쓴다.
